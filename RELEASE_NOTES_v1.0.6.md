# Border Lab v1.0.6 更新说明

## 参数记忆与批量同步

- 导入新照片时继承当前手动调整的参数，不再自动重新套用模板默认值
- 仅在主动点击模板或恢复默认时应用模板参数
- 右上角「同步全部照片」改为持续开关：开启后黄色常亮，之后的参数修改实时同步到全部照片；再次点击即可关闭
- 每张照片独立保存方向、手动旋转和设计参数

## 预览与批量导出一致性

- 修复横图、竖图混合批量导出时，未逐张打开的照片会在导出阶段临时应用方向模板，导致底部文字大小和位置与预览不同的问题
- 预览和导出统一使用逐照片渲染状态
- 移除可能污染其他照片方向和参数的后台预览预热路径
- JPEG 导出继续保留 EXIF，并将成品方向标记归一为正常方向

## 预览文字原位编辑

- 双击预览中的主标题、副标题、EXIF 信息或品牌机型文字即可直接修改
- 扩大整行文字命中区域，小字体和缩放状态下也更容易双击选中
- 鼠标进入可编辑区域时显示文字光标
- 编辑界面采用透明原位文字，不显示额外输入框或虚线

## 界面与性能

- 修复滑块轨道填充在窗口重排时出现断续白块的问题
- 窗口启动后立即显示，并关闭不必要的拼写检查
- Windows 便携版采用快速无压缩封装，减少每次启动时的解压等待

## 下载

- **Windows x64 便携版**：`Border Lab 1.0.6.exe` — 无需安装，双击即用

## 建议重点测试

1. 调整滑块后导入新照片，确认新照片保留当前参数
2. 混合导入横图和竖图并批量导出，对比底部文字大小、位置与预览
3. 开启黄色「同步全部照片」按钮后连续调整参数，再关闭并单独修改一张照片
4. 双击不同文字行，确认能稳定进入原位编辑且导出文字完整

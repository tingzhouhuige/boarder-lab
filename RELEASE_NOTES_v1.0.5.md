# Border Lab v1.0.5 更新说明

## 批量编辑与设置记忆

- 每张照片独立记忆边框、文字、颜色、字体、位置和水印参数，切换照片不再恢复模板默认值
- 「应用到全部照片」按钮改为一次性复制当前设置，并增加明确的高亮反馈
- 批量导出会逐张读取各自设置，导出前自动保存当前照片的最新修改
- 未打开过的批量照片也会在导出前完成横图／竖图默认参数初始化

## 横竖图默认参数

- 照片首次导入并读取尺寸及 EXIF 方向后，自动匹配横图或竖图对应的预设参数
- 默认参数只初始化一次；后续切换照片恢复该照片保存的参数，不再反复覆盖
- 批量应用后的照片会保留统一设置，不会再次被方向预设替换
- 旋转按钮只负责旋转预览，不触发横竖图默认参数重置

## UI 与滑块优化

- 修复滑块达到 `100%` 时数值或标题换行、挤压左侧面板布局的问题
- 修复滑块圆点与黄色轨道填充位置不重合的问题
- 窗口尺寸变化后自动重新计算滑块填充位置
- 统一批量应用按钮的悬停、键盘焦点和操作反馈样式

## 模板与 EXIF 修复

- 修复暗色画廊、签名水印及品牌水印模板切换时部分控件显隐错误的问题
- 品牌水印允许自由混用标识；存在 EXIF 时，下方机型文字显示照片的真实相机型号
- 修复带 EXIF 方向信息的照片缩略图方向可能错误的问题
- JPEG 导出时保留相机 EXIF，同时把方向标记归一并更新成品尺寸，避免部分看图软件二次旋转
- 圆角 PNG／普通 JPEG 的导出格式改为统一的全局导出选项，不随单张照片切换

## 下载

- **Windows x64 便携版**：`Border Lab 1.0.5.exe` — 无需安装，双击即用

## 建议重点测试

1. 同时导入横图和竖图，确认首次打开时分别套用对应预设
2. 分别调整多张照片后反复切换，确认每张照片的设置保持不变
3. 点击「应用到全部照片」后检查所有照片及批量导出结果
4. 将位置滑块拖到 `100%`，确认标题、数值和滑轨均不发生错位
5. 使用带 EXIF 方向的手机或相机 JPEG，检查缩略图、预览和导出方向
